import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.baituman.routeplanner',
  appName: 'Route Planner',
  webDir: 'dist'
};

export default config;
